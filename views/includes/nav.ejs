<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> </title>
    <style>
           * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            display: flex;
            background-color: #f0f2f5;
            color: #333;
        }

        .sidebar {
            width: 250px;
            background-color: #ffffff;
            height: 100vh;
            padding: 24px;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.05);
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 40px;
            color: #4285f4;
        }

        .nav-item {
            display: flex;
            align-items: center;
            padding: 12px 16px;
            margin: 8px 0;
            color: #666;
            cursor: pointer;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .nav-item:hover {
            background-color: #f0f7ff;
            color: #4285f4;
        }

        .logout {
            color: #ff4444;
             
        }
        a
        {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="sidebar">
       <div class="logo">PrintTrack</div>
       <a href="/"> <div class="nav-item">📊 Dashboard</div></a>
        <div class="nav-item">📦 Orders</div>
        <div class="nav-item">📄 Invoices</div>
        <a href="/profile"> <div class="nav-item">👤 Profile</div></a>
        <a href="/login"><div class="nav-item">🚪Login</div></a>
        <div class="nav-item logout" >🚪 Logout</div>
    </div>
</body>
</html>