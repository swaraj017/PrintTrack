<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Keep existing styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: #f0f2f5;
            color: #333;
            display: flex;
            min-height: calc(100vh - 15px); /* Reduced height by 15px */
        }

        .main-content {
            flex: 1;
            padding: 2rem 2rem 1rem; /* Reduced bottom padding */
            margin-left: 30px;
        }

        .profile-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            padding: 2rem 2rem 1.5rem; /* Reduced bottom padding */
            max-width: 1000px;
            margin: 0 auto;
        }

        .profile-header {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            margin-bottom: 1.5rem; /* Reduced margin */
            padding-bottom: 1.5rem; /* Reduced padding */
            border-bottom: 1px solid #eee;
        }

        /* Rest of the styles remain the same */
        .profile-avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            margin-bottom: 1.5rem;
            border: 5px solid #4285f4;
            padding: 5px;
        }

        .profile-name {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 0.5rem;
        }

        .profile-title {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            gap: 2rem;
            
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            color: #666;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .contact-item:hover {
            background: #f8fafc;
        }

        .contact-item i {
            font-size: 1.2rem;
        }

        .fa-envelope { color: #EA4335; }
        .fa-phone { color: #34A853; }
        .fa-map-marker-alt { color: #4285F4; }

        .profile-bio {
            text-align: center;
            max-width: 600px;
            margin: 0.8rem auto; /* Reduced margin */
            line-height: 1.6;
            color: #666;
        }

        .profile-address {
            display: flex;
            align-items: center;
            gap: 0.8rem;
         
            padding: 0.5rem 1rem;
            border-radius: 8px;
            color: #666;
            transition: all 0.3s ease;
        }

        .profile-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem; /* Reduced margin */
        }

        .stat-card {
            background: #f8fafc;
            padding: 1.2rem; /* Slightly reduced padding */
            border-radius: 15px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            background: #f0f7ff;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: #4285f4;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #666;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .stat-icon {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .fa-print.stat-icon { color: #4285F4; }
        .fa-users.stat-icon { color: #34A853; }
        .fa-star.stat-icon { color: #FBBC05; }
        .fa-calendar-check.stat-icon { color: #EA4335; }

        @media (max-width: 768px) {
            .main-content {
                margin-left: 0;
                padding: 1rem 1rem 0.5rem; /* Reduced padding for mobile */
            }
        }
    </style>
</head>
<body>
    <%- include("includes/nav.ejs") %>
    <div class="main-content">
        <div class="profile-card">
            <div class="profile-header">
                <img src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="Profile Picture" class="profile-avatar">
                <h1 class="profile-name"><%= user.name %></h1>
                <div class="profile-title">Senior Print Designer</div>
                
                <div class="contact-info">
                    <div class="contact-item">
                        <i class="fas fa-envelope"></i>
                        <%= user.email %>
                     
                        <i class="fas fa-phone"></i>
                        <%= user.contactNumber %>
                    </div>
                </div>
                <div class="profile-address">
                    <i class="fas fa-map-marker-alt"></i>
                    <%= user.address %>
                </div>
            </div>

            <div class="profile-stats">
                <div class="stat-card">
                    <i class="fas fa-print stat-icon"></i>
                    <div class="stat-value">1,234</div>
                    <div class="stat-label">Total Prints</div>
                </div>
                <div class="stat-card">
                    <i class="fas fa-users stat-icon"></i>
                    <div class="stat-value">89</div>
                    <div class="stat-label">Active Clients</div>
                </div>
                <div class="stat-card">
                    <i class="fas fa-star stat-icon"></i>
                    <div class="stat-value">4.9</div>
                    <div class="stat-label">Rating</div>
                </div>
                <div class="stat-card">
                    <i class="fas fa-calendar-check stat-icon"></i>
                    <div class="stat-value">98%</div>
                    <div class="stat-label">On-Time Delivery</div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>